# M2 人工体验 P0 复验记录（2026-08-25）

## 复验目标

修复技能启动时将内部结构化上下文显示在聊天记录中的 P0 沉浸感问题。

## 修复结果

- 技能请求继续将完整结构化上下文提供给真实模型，用于安全边界与结果生成。
- 写入并展示给用户的消息改为自然启动语，例如“我想体验一次塔罗趣味解读。”。
- Mock 回复识别技能上下文后不再复述玩法规则、固定说明或用户原始输入。
- 结构化结果卡仍保留“趣味解读，仅供娱乐参考”标识。

## 自动复验

- 完整验证：481 项单元、组件与安全测试通过。
- 隔离浏览器 E2E：6/6 通过。
- 技能 E2E 新增断言：聊天记录包含自然启动语，且不包含“玩法边界”“固定说明”。
- 星座 E2E 验证“5月20日”可完成技能会话；服务端也兼容 `05-20`、`5-20` 与不带“日”的中文输入。

## 人工观察补充

修复前已在本地人工验收中确认用户气泡可显示自然启动语；随后发现 Mock 回复仍可能摘录内部上下文，已作为同一 P0 的补充修复，并由上述浏览器 E2E 覆盖复验。

Chrome 本次复验时扩展可发现标签但无法附加调试会话，因此未重复执行人工点击；该限制不影响隔离 E2E 的实际浏览器验收。

## 结论

该 P0 已关闭，星座日期兼容性 P1 也已关闭。Mock 文案质量与桌面端留白属于后续模型接入与视觉设计任务，仍保留为非阻塞项。
