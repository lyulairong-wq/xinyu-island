# M7 外部封测发布工程化验收报告

**状态：** 代码与本地自动化验收完成；尚未合并、推送或对外开放。

## 本阶段交付

- 生产配置锁定：仅允许 HTTPS 来源、阿里云百炼北京兼容端点与指定模型；强制邀请制、成年人确认、反馈入口、禁用 Mock 回退、Token 模式和应用入口。
- 应急暂停：运行中可以显式关闭注册或生成；`beta:preflight` 会拒绝将暂停状态用于发布，避免误开放。
- 容器部署：提供 API、Web、迁移和 Caddy Compose 模板；仅 Caddy 暴露 80/443，数据库只通过私网 `DATABASE_URL` 接入。
- 运维脚本：发布前证据目录检查、加密 PostgreSQL 备份、仅允许隔离库的恢复演练保护。
- 自动门禁：新增 GitHub Actions，在 M7 分支/合并请求中执行完整验证、运维脚本、Compose 静态校验及三个镜像目标构建；工作流不部署、不读取真实密钥。

## 已获得的本地证据

| 检查 | 结果 |
| --- | --- |
| `npm run verify` | 通过：API 175、Web 50、AI 7、配置 28、Contracts 1、安全 305 项测试，以及全量构建。 |
| `npm run test:ops` | 通过：6 项发布预检、备份和恢复保护测试。 |
| `docker compose --env-file deployment/external-beta.env.example -f deployment/compose.external-beta.yml config --quiet` | 通过。 |
| `npm audit --omit=dev --json` | 生产依赖 0 个漏洞。 |
| Webpack 生产构建 | 通过；容器构建同样显式采用 Webpack，避免本机 Docker Desktop 中 Turbopack 构建器挂起。 |

## 合并前 CI 门槛

本机 Docker Desktop 在 Node/TypeScript 容器编译中出现过 V8 异常与无进度挂起，不能作为“完整镜像已验证”的有效证据。M7 已将完整的 `migrate`、`api`、`web` 镜像构建放入 GitHub Actions；在分支推送后，三项构建全部通过才满足 M7 的合并条件。

## 对外邀请前仍须人工完成的门槛

以下事项不由代码或 CI 替代，未完成前不得创建邀请码或开放注册：

1. 协议、隐私政策与娱乐提示的专业审阅留档。
2. 中国大陆主体、ICP备案、HTTPS 与公安备案计划的合规确认。
3. 北京地域私网 PostgreSQL、模型密钥、访问控制与预算/告警的实际配置。
4. 以真实但脱敏的封测环境执行一次加密备份与隔离库恢复演练，并保存证据。
5. 在仓库外准备 `BETA_RELEASE_EVIDENCE_DIR` 后，以真实生产环境变量运行 `npm run beta:preflight`。

完成 CI 与上述人工门槛后，才可进入邀请外部用户的受控封测流程。
