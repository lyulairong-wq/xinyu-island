# M2 聊天与趣味技能验证记录

## 验收清单

- [ ] 未登录请求与跨用户资源访问均被服务端拒绝。
- [ ] 官方联系人不可被编辑或删除；私有联系人仅可由本人管理，并受最多 3 个已审核技能及 1 个主技能限制。
- [ ] 平台仅提供塔罗、MBTI、星座、紫微斗数、梅花易数五项技能。
- [ ] 高风险话题在生成、扣减额度或持久化前被安全策略拒绝。
- [ ] 群聊技能必须由用户显式选择具备该技能的成员，并将该成员作为目标。
- [ ] 聊天支持引用、复制、重新生成与继续；历史不会被静默覆盖；草稿按会话隔离。
- [ ] 桌面双栏与移动单栏导航均可用；会话归档/删除需明确确认。
- [ ] 技能卡片固定展示“趣味解读，仅供娱乐参考”，且不显示原始出生信息等输入。
- [ ] 技能事实仅在用户显式选择并输入内容后保存至对应 AI 联系人；可删除。
- [ ] 免费/Token 模式延续开发模拟额度语义，不含充值、支付或真实扣费路径。
- [x] 全量自动化、生产构建和全新数据库迁移均通过。

## 执行证据

### 自动化与构建

2026-08-18 在 M2 隔离工作树执行：

```powershell
npm.cmd run verify
```

结果：通过。包含 API 16 个测试文件 / 134 项测试、Web 16 个测试文件 / 33 项测试、AI 6 项测试、Config 13 项测试、Safety 305 项测试、所有工作区类型检查，以及 API/Web 生产构建。

### 数据库迁移

2026-08-19 使用名为 `xinyu-m2-verify` 的一次性 PostgreSQL 16 容器、宿主机 `15432` 端口执行：

```powershell
npm.cmd run db:generate
npx.cmd prisma migrate deploy --schema apps/api/prisma/schema.prisma
npx.cmd prisma migrate status --schema apps/api/prisma/schema.prisma
```

结果：通过。9 条迁移从空数据库全部成功部署，随后 Prisma 报告 `Database schema is up to date!`。验证容器已删除，未改动任何用户已有的数据库或容器。

### 浏览器关键路径

组件级自动化已覆盖草稿隔离、引用/重新生成、联系人与群聊上限、技能菜单、一次性提示、卡片脱敏与群聊目标成员。完整浏览器手工路径（桌面与窄屏登录、五项技能、记忆保存/删除、归档/删除）待可运行 API 与浏览器会话的环境中执行并补充截图或步骤证据。

2026-08-19 复核时，浏览器自动化运行时未发现可用浏览器实例，因而未替代为其他控制方式。该项继续作为合并前环境验收缺口。
