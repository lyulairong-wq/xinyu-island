# 心屿（Island）项目 Agent 宪法

本文件适用于 `xinyu-island/` 及其所有子目录。它规定 AI Agent 在本仓库中的工作边界、开发流程和验证门槛。

## 1. 规则优先级与事实来源

1. 系统、开发者和用户的直接指令优先于本文件。
2. 产品事实以 `docs/` 为准；实现与文档冲突时，先报告冲突，不得自行改变产品方向。
3. 需求和验收的权威文件：
   - `docs/心屿产品需求基线_PRD.md`
   - `docs/最小可用版本验收标准.md`
   - `docs/心屿API契约与错误码设计.md`
   - `docs/心屿核心数据模型设计.md`
   - `docs/模块化开发与集成策略.md`
   - `docs/模块需求与验收模板.md`
4. 本文件只规定工程工作方式；不能取代正式用户协议、隐私政策或专业法律审核。

## 2. 产品不可违反边界

- 心屿是面向中国大陆中文用户的娱乐陪伴 Web 产品，不提供医疗、心理诊断、法律、财务或其他专业服务，也不替用户作现实决策或预测现实人生。
- 高风险内容必须经过 `packages/safety` 的统一安全边界；任何 AI 人格、Prompt、应用模块或前端界面都不得绕过该边界。
- 长期记忆按“用户 × AI 联系人”隔离，默认关闭；不同 AI 联系人不得共享用户记忆；敏感记忆不得自动注入生成上下文。
- 用户数据只能由所属用户访问。涉及聊天、联系人、记忆、额度、导出或删除时，必须验证所有权和授权状态。
- 未经用户明确确认，不得定稿或替换最终 UI、视觉规范、官方 AI 人格、人生镜像剧情、正式协议文本、隐私政策或支付规则。
- 真实 Token 充值、支付、订阅、公开注册和公开发布不属于当前免费封闭测试 MVP 范围。

## 3. 当前目标与范围控制

- 当前首要产品节点是“免费封闭测试 MVP”，完整标准见 `docs/最小可用版本验收标准.md`。
- 一次只开发一个功能模块。不得在同一变更中混入无关重构、未确认 UI、角色文案、剧情内容或依赖升级。
- 当任务含义不明确且会改变产品范围、数据语义、法律风险或外部成本时，停止并请求确认；低风险实现细节可作最小、可逆假设，并在交付中说明。
- 优先复用已有模块、共享契约和测试工具；同一业务规则出现第三次稳定重复时，再提取共享能力。

## 4. 模块开发流程

每个模块按以下顺序执行：

1. 从当前基线创建 `feature/<module-name>` 分支，不直接在 `main` 上混合开发。
2. 使用 `docs/模块需求与验收模板.md` 明确目标、非目标、数据变化、API、权限、安全边界和验收用例。
3. 实现模块本身及必要测试；共享契约变更必须明确影响范围。
4. 先通过模块测试，再执行真实 PostgreSQL API 验证；涉及界面时再完成浏览器 E2E 验证。
5. 以小而完整的提交保存变更和验证结果。
6. 模块通过后进入集成分支，执行全量回归、迁移检查与跨模块关键路径测试；仅通过集成测试后才可合入 `main`。

未得到明确授权时，不执行外部发布、推送、支付、通知、删除用户数据或其他不可逆外部操作。

## 5. 架构与代码边界

```text
apps/web        用户界面与浏览器交互
apps/api        Controller、DTO、业务服务和数据库调用
packages/contracts  前后端共享类型、接口契约和错误码
packages/safety     统一安全边界
packages/ai         模型 Provider 适配与降级
packages/config     配置校验
```

- Controller 只负责请求/DTO/身份传递/响应；业务规则放在 Service。
- Service 负责权限、业务流程、事务和共享能力协调；不要在页面或 Controller 复制额度、安全、记忆或模型规则。
- `packages/*` 只放跨模块稳定能力。修改共享包前先检查所有消费者并执行相关回归测试。
- 优先清晰、显式、可测试的实现；避免通过大量布尔参数、隐式全局状态或跨模块直接访问数据库来控制业务分支。
- 不以“顺手整理”为理由重构无关文件；若重构必要，应与功能变更拆分或明确说明原因。

## 6. 数据库、迁移与数据安全

- 已提交的 Prisma migration 永不修改、删除或重写；每次结构变化必须新增命名清楚的 migration。
- 每条 migration 必须能在新数据库按顺序执行；涉及现有数据时必须说明兼容、回滚和恢复影响。
- 删除聊天不得隐式删除独立的额度/用量流水；删除或重置记忆不得影响其他联系人或其他用户数据。
- 执行 `prisma migrate reset`、删除卷、清库、批量删除或其他破坏性操作前，必须确认精确目标、备份状态和用户授权。
- 不提交 `.env`、API Key、数据库密码、生产令牌、完整聊天内容或其他敏感数据。

## 7. 模型、安全与额度

- 免费模式优先使用配置的真实开源模型；Mock 仅为 Provider 不可用时的保底，不得伪装成真实模型回复。
- 真实远程 Provider、长期记忆和 Token 消耗均须得到对应用户授权；模型请求只发送必要上下文。
- 额度扣减必须可追溯、可测试，并为幂等、失败和回滚预留正确流程；删除聊天不应改变历史用量记录。
- 安全事件、普通运行日志和开发调试输出均不得暴露 API Key、完整聊天正文或敏感记忆。

## 8. 测试与完成门槛

优先使用项目已有脚本，而不是临时拼接测试命令：

```powershell
npm.cmd run test:api
npm.cmd run test:web
npm.cmd run test:ai
npm.cmd run test:safety
npm.cmd run verify
```

- 修改 API、数据模型、权限、安全、额度或模型逻辑时，必须增加或更新对应单元/API 测试。
- 修改 Web 交互时，必须验证加载、成功、失败、空状态和核心用户路径；关键流程增加浏览器 E2E。
- 模块完成前至少验证正常路径、权限拒绝、异常/重试、删除/撤回和安全边界。
- 变更完成前运行与风险相称的验证，并报告实际执行结果、未验证项和已知限制；不得声称未运行的测试已通过。
- 合并前必须通过 `npm.cmd run verify`，并用以下方式确认数据库迁移状态：

```powershell
$env:DATABASE_URL='postgresql://xinyu:xinyu@localhost:5432/xinyu_island?schema=public'
npx.cmd prisma migrate status --schema apps/api/prisma/schema.prisma
```

## 9. Git、文档与可追溯性

- 每个提交只包含一个清晰、可验证的目的；迁移、实现、测试和必要文档可在同一模块提交中出现。
- 提交前检查工作区与 diff，保护用户已有的无关变更；禁止使用 `git reset --hard`、强制覆盖或未经确认的历史改写。
- 需求、接口、数据模型、验收标准或运行方式发生实质变化时，同步更新对应 `docs/` 文件。
- 每个模块完成后记录验证结果和遗留限制；公开测试必须额外满足 `docs/公开测试前准备清单.md`。

## 10. 宪法维护

- 修改本文件属于项目工作流变更，必须说明原因、影响范围和验证方式。
- 本文件保持精简、可执行、项目专属。复杂或模块专属规则应写入 `docs/` 或未来对应目录的 `AGENTS.md`，不要无限扩张根文件。
